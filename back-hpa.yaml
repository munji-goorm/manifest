apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: munji-back
  namespace: munji
  labels:
    app: munji-back
spec:
  maxReplicas: 6
  minReplicas: 2
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: munji-back
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 80
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
status:
  currentReplicas: 0
  desiredReplicas: 3
